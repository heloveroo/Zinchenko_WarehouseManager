<Window x:Class="WarehouseManager.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Менеджер складів"
        Width="900" Height="650"
        MinWidth="700" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <!-- Хедер застосунку -->
            <RowDefinition Height="56"/>
            <!-- Основний вміст — Frame для навігації між сторінками -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── Хедер ── -->
        <Border Grid.Row="0"
                Background="{StaticResource PrimaryBrush}">
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="24,0">
                <TextBlock Text="📦"
                           FontSize="22"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                <TextBlock Text="Менеджер складів"
                           Foreground="White"
                           FontSize="18"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- ── Frame для навігації між сторінками ──
             NavigationUIVisibility="Hidden" — приховує стандартну панель навігації WPF,
             бо ми реалізуємо власні кнопки "Назад" на кожній сторінці. -->
        <Frame x:Name="MainFrame"
               Grid.Row="1"
               NavigationUIVisibility="Hidden"
               Background="{StaticResource BackgroundBrush}"/>
    </Grid>
</Window>
